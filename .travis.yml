language: generic
sudo: required

services:
    - docker
before_install:
    docker build -t kartik27/docker-app -f Dockerfile.dev .
script:
    - docker run -e CI=true kartik27/docker-app npm run test